# Screenshots

Drop the release screenshots here (referenced by the root `README.md` and
`docs/index.html`). Suggested set:

- `visualizer.png` - Awesome mode (Ctrl+T) sub-cell block visualizer on a **Linux**
  256-color terminal (the 256-color visuals are the highlight; Windows/MSYS2 reports
  `COLORS=8`, so capture this one on Linux).
- `playlist.png` - the two-pane browser + playlist view during playback.
- `ripping.png` - the CD rip panel / AccurateRip status (optional third shot).

> Placeholder: no images are committed yet - capture from a real terminal session.
> Keep them reasonably sized (e.g. ≤ 1600px wide, PNG) so the repo stays light.
